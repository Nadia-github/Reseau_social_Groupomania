<template>
    <h1> Bienvenue à toi cher collègue</h1>

</template>

<script>

    const userId = window.localStorage.getItem("userId");
    const token = window.localStorage.getItem("userToken")

    /*export default post (url, data = {}, config = {}) 
        {return request({
        ...config,
        method: 'POST',
        url,
        data
        });
    }*/

    const userData = {
        prenom,
        nom,
        sexe,
        email
    }
    
    export default profil-acces;
</script>